/**
 * That enum file, should describe all socket messages. Example:
 * 
 * ADD_LIST is the event fired when some list is added (ever in infinitive)
 * 
 */
const SocketActions = {
  ADD_LIST: 'add_list',
  DELETE_LIST: 'delete_list',
  UPDATE_LIST: 'update_list',
}

module.exports = SocketActions;